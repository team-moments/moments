<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.moments.users.UsersMapper">   
	<insert id="userInsert" parameterType="kr.co.moments.domain.UsersVO">
		insert into tb_users (
			 users_email, users_pwd, users_name, users_phone
		) values (
			#{users_email}, #{users_pwd}, #{users_name}, #{users_phone}
		);
	</insert>
	<select id="findByUserEmail" resultType="kr.co.moments.domain.UsersVO" parameterType="String" >
		select *
		from tb_users
		where users_email = #{user_email};
	</select>
	<!-- 리프레시 토큰 저장 -->
	<update id="updateRefreshToken" parameterType="String">
	    UPDATE tb_users
	    SET refresh_token = #{refreshToken}
	    WHERE users_email = #{email}
	</update>
	
	  <!-- 리프레시 토큰 삭제 (로그아웃) -->
	<update id="deleteRefreshToken" parameterType="String">
	    UPDATE tb_users
	    SET refresh_token = NULL
	    WHERE users_email = #{email}
 	</update>
</mapper>